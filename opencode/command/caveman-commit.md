---
description: Generate a terse conventional commit message
agent: build
argument-hint: "[optional diff context]"
---

Use `caveman-commit`.

Generate commit message only. Do not stage or commit.

Context:
$ARGUMENTS

