---
description: Debug with persistent GSD-lite session state
agent: gsd-lite-debugger
argument-hint: "[list | status <slug> | continue <slug> | bug description]"
---

Use `gsd-lite-debug`.

Persist state in `.planning/debug/{slug}.md`. Read `.planning/current/*` if present. Write compact latest state to `.planning/current/HANDOFF.md` when central context exists.

Use Matt `diagnose` method: reproduce, hypothesize, test, evidence, fix only after root cause.

Style: structured terse caveman.

Input:
$ARGUMENTS
