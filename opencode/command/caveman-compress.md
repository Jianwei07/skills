---
description: Compress a natural language memory file with caveman rules
agent: build
argument-hint: "<filepath>"
---

Use `caveman-compress`.

Compress only supported natural language files. Preserve code blocks, inline code, URLs, paths, commands, dates, and structure exactly where required.

File:
$ARGUMENTS

