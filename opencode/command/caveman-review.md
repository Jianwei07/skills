---
description: Write terse actionable code review comments
agent: build
argument-hint: "[PR, diff, or files]"
---

Use `caveman-review`.

Findings only. One line per actionable issue unless security/architecture needs detail.

Scope:
$ARGUMENTS

