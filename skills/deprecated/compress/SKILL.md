---
name: compress
description: Legacy alias for caveman-compress. Deprecated because it duplicated the full caveman-compress implementation.
---

# Compress (deprecated)

This skill is deprecated.

Use [`caveman-compress`](../../core/caveman-compress/SKILL.md) instead.

Why deprecated:

- `compress` and `caveman-compress` had identical helper scripts.
- Keeping both made the repo noisier without adding a different workflow.
- The canonical maintained version is now `skills/core/caveman-compress/`.
